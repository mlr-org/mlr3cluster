# nrep combined with cluster errors

    Code
      learner$train(task)
    Condition
      Error:
      ! 
      x `nrep` requires random initialization and cannot be combined with `cluster`.
      > Class: Mlr3ErrorConfig

